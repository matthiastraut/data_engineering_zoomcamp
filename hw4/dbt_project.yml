name: "taxi"
version: "1.0.0"
config-version: 2

# Require a specific dbt version for reproducibility
require-dbt-version: [">=1.7.0", "<2.0.0"]

# This setting configures which "profile" dbt uses for this project.
profile: "taxi"

# These configurations specify where dbt should look for different types of files.
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

# Project-level variables
vars:
  # Set to false to run the full 2019-2020 dataset
  is_test_run: false

# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models
models:
  taxi:
    staging:
      +materialized: view
    #marts:
    #  +materialized: table
# Configuring seeds
#seeds:
#  taxi:
#    taxi_zone_lookup:
#      +column_types:
#        locationid: numeric
